// @ts-nocheck
// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { Ack, AckT } from '../furnace/ack.js';
import { DebugInfoResponse, DebugInfoResponseT } from '../furnace/debug-info-response.js';
import { Error, ErrorT } from '../furnace/error.js';
import { HistoryResponse, HistoryResponseT } from '../furnace/history-response.js';
import { LogContentResponse, LogContentResponseT } from '../furnace/log-content-response.js';
import { LogListResponse, LogListResponseT } from '../furnace/log-list-response.js';
import { PreferencesResponse, PreferencesResponseT } from '../furnace/preferences-response.js';
import { ProgramContentResponse, ProgramContentResponseT } from '../furnace/program-content-response.js';
import { ProgramListResponse, ProgramListResponseT } from '../furnace/program-list-response.js';
import { ServerMessage, unionToServerMessage, unionListToServerMessage } from '../furnace/server-message.js';
import { State, StateT } from '../furnace/state.js';


export class ServerEnvelope implements flatbuffers.IUnpackableObject<ServerEnvelopeT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ServerEnvelope {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsServerEnvelope(bb:flatbuffers.ByteBuffer, obj?:ServerEnvelope):ServerEnvelope {
  return (obj || new ServerEnvelope()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsServerEnvelope(bb:flatbuffers.ByteBuffer, obj?:ServerEnvelope):ServerEnvelope {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ServerEnvelope()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

requestId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

messageType():ServerMessage {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : ServerMessage.NONE;
}

message<T extends flatbuffers.Table>(obj:any):any|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__union(obj, this.bb_pos + offset) : null;
}

static startServerEnvelope(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addRequestId(builder:flatbuffers.Builder, requestId:number) {
  builder.addFieldInt32(0, requestId, 0);
}

static addMessageType(builder:flatbuffers.Builder, messageType:ServerMessage) {
  builder.addFieldInt8(1, messageType, ServerMessage.NONE);
}

static addMessage(builder:flatbuffers.Builder, messageOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, messageOffset, 0);
}

static endServerEnvelope(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishServerEnvelopeBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset);
}

static finishSizePrefixedServerEnvelopeBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, undefined, true);
}

static createServerEnvelope(builder:flatbuffers.Builder, requestId:number, messageType:ServerMessage, messageOffset:flatbuffers.Offset):flatbuffers.Offset {
  ServerEnvelope.startServerEnvelope(builder);
  ServerEnvelope.addRequestId(builder, requestId);
  ServerEnvelope.addMessageType(builder, messageType);
  ServerEnvelope.addMessage(builder, messageOffset);
  return ServerEnvelope.endServerEnvelope(builder);
}

unpack(): ServerEnvelopeT {
  return new ServerEnvelopeT(
    this.requestId(),
    this.messageType(),
    (() => {
      const temp = unionToServerMessage(this.messageType(), this.message.bind(this));
      if(temp === null) { return null; }
      return temp.unpack()
  })()
  );
}


unpackTo(_o: ServerEnvelopeT): void {
  _o.requestId = this.requestId();
  _o.messageType = this.messageType();
  _o.message = (() => {
      const temp = unionToServerMessage(this.messageType(), this.message.bind(this));
      if(temp === null) { return null; }
      return temp.unpack()
  })();
}
}

export class ServerEnvelopeT implements flatbuffers.IGeneratedObject {
constructor(
  public requestId: number = 0,
  public messageType: ServerMessage = ServerMessage.NONE,
  public message: AckT|DebugInfoResponseT|ErrorT|HistoryResponseT|LogContentResponseT|LogListResponseT|PreferencesResponseT|ProgramContentResponseT|ProgramListResponseT|StateT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const message = builder.createObjectOffset(this.message);

  return ServerEnvelope.createServerEnvelope(builder,
    this.requestId,
    this.messageType,
    message
  );
}
}
